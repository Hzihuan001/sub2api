# v0.2.5 代理协议本地验收记录

日期：2026-09-18

## 验收对象

- 主站：`0.2.5-custom.15`
- L1 / COS：`0.2.5-l1.5`
- 官方合并基线：`v0.2.5` (`4af0e80db1b0bc7626dfb8fb76ccaffc6bb0dc17`)
- Compose 项目：`moshu-reseller-acceptance`

所有测试均使用隔离 PostgreSQL、Redis、应用卷、合成账号和本地模拟上游，没有连接生产数据库或生产模型服务。

## 本地地址

- 主站：<http://127.0.0.1:18180>
- L1：<http://127.0.0.1:18181>
- COS：<http://127.0.0.1:18182>

环境暂时保留。停止应用但保留数据：

```powershell
docker compose -p moshu-reseller-acceptance -f deploy/docker-compose.reseller-acceptance.yml stop
```

## 自动化结果

- 主站：`internal/reseller`、`internal/authz`、管理认证中间件、管理员处理层及服务层测试通过。
- 代理站：`internal/moshureseller`、仓储、服务层及管理员处理层测试通过。
- 前端：5 个相关测试文件共 30 个测试通过；类型检查、ESLint、i18n 键完整性检查和 Vite 生产构建通过。
- 两端生产 Docker 镜像构建通过；Compose 配置和 `git diff --check` 通过。
- 三个应用容器均 healthy、重启次数为 0，SPA 入口及哈希 JS 资源返回 200。
- 最近日志未发现 panic、fatal、迁移失败、数据库或 Redis 连接错误。

## 联合验收结果

- 同一主站计费账号成功绑定两个独立代理商，L1 和 COS 的授权、产品、模型、凭证和数据互相隔离。
- DeepSeek 风格和 OpenAI 风格账号测试、模型目录、JSON 网关请求及 SSE 请求通过。
- 主站分组逐模型价、主站账号实际倍率、代理站本地逐模型价、用户专属倍率及零倍率路径均按既定优先级生效。
- 主站改价后两个代理自动取得新版本；代理站本地名称、倍率、容量和模型白名单未被覆盖。
- 同一上游账号关联多个本地分组后，实际请求仍按所选账号的主站成本快照结算。
- schema 1 与 schema 2 使用独立缓存；schema 2 校验准确覆盖原始 payload，损坏缓存会绕过 304 并完整修复。
- 主站短时断开并重启代理后，代理可使用同租户有效缓存继续请求；主站恢复后同步恢复。
- 结算事件重复、迟到和修订测试通过。最终主站成本只更新管理员成本字段，用户余额、用户消耗、订阅和配额未被二次修改。
- 主站事件积压为 0；L1 和 COS 均持久化确认状态、估算成本、实际成本及差额。
- 精确旧代理镜像 `0.2.4-l1.5` 可在新增迁移后的数据库上启动，并可使用 schema 1 缓存完成请求；恢复新镜像后健康正常。

## 发布结果

- 主站提交：`fef9dc01a`，标签：`custom-0.2.5.15`，镜像 digest：`sha256:76253a08632f7e62957455f150107b55ad703dda909c3de5791e61b9d76b1329`。
- 代理站提交：`de11106a8`，标签：`custom-l1-0.2.5.5`，L1 / COS 共用镜像 digest：`sha256:8f43ef71feb90489a9bf122b8b1e69dfda340a1348d986843bf7e7c41938ebdd`。
- 主站、L1、COS 均为 healthy、重启次数为 0，三个公网 `/health` 返回成功，管理 SPA 返回 200。
- L1 / COS 的 `238` 历史迁移校验值保持不变，新增 `239`、`240` 迁移成功；最近日志无 panic、fatal、迁移、数据库或 Redis 关键错误。
- L1 的授权、目录、价格和结算同步均成功且无错误；主站对 L1 的 32 条结算事件已全部确认，COS 当前无结算事件积压。

## 历史七天只读对账

- L1：共 4,173 条主站结算，其中 343 条没有本地使用记录，但不存在引用了缺失使用记录的结算行。
- L1 主站实际成本为 `$23.6495654100`，本地管理员成本为 `$85.3252700400`，绝对差额为 `$61.7294405100`。
- COS：最近七天无结算记录。
- 历史数据仅报告差异，未批量回写，也未修改用户余额、消耗、订阅或配额。

## 说明

- 本轮仅对上线后请求自动进行结算事件处理；本地环境没有上线前七天的历史生产记录。
- 浏览器权限、菜单和控件由相关组件及路由测试覆盖；三个本地站点保留用于人工页面复核。
- Vite 报告的既有大 chunk 和动态/静态导入提示不影响构建成功，未在本轮扩大范围处理。
